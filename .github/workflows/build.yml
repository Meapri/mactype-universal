name: Build MacType

on:
  push:
    branches: [ "main", "directwrite", "develop" ]
  pull_request:
    branches: [ "main", "directwrite" ]
  workflow_dispatch:

concurrency:
  group: build-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  SOLUTION_FILE_PATH: ./gdipp.sln

jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 90

    strategy:
      fail-fast: false
      matrix:
        platform: [x64]

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup MSBuild
      uses: microsoft/setup-msbuild@v2

    - name: Add Windows SDK to PATH and set compatible SDK version
      run: |
        echo "=== 설치된 Windows SDK 버전들 ==="
        $sdkVersions = Get-ChildItem "${env:ProgramFiles(x86)}\Windows Kits\10\Include\" -Directory -ErrorAction SilentlyContinue | Sort-Object Name
        $sdkVersions | ForEach-Object { echo "  - $($_.Name)" }
        $preferredVersions = @("10.0.26100.0", "10.0.22621.0", "10.0.22000.0", "10.0.20348.0", "10.0.19041.0")
        $selectedSdk = $null
        foreach ($version in $preferredVersions) {
          $sdkIncludePath = "${env:ProgramFiles(x86)}\Windows Kits\10\Include\$version"
          if (Test-Path $sdkIncludePath) { $selectedSdk = $version; echo "선택된 Windows SDK: $version"; break }
        }
        if (-not $selectedSdk -and $sdkVersions) {
          $newestSdk = $sdkVersions | Where-Object { $_.Name -match "^\d+\.\d+\.\d+\.\d+$" } | Sort-Object Name -Descending | Select-Object -First 1
          if ($newestSdk) { $selectedSdk = $newestSdk.Name; echo "최신 Windows SDK 사용: $selectedSdk" } else { $selectedSdk = $sdkVersions[0].Name; echo "기본 Windows SDK 사용: $selectedSdk" }
        }
        if ($selectedSdk) {
          echo "WindowsSDKVersion=$selectedSdk" >> $env:GITHUB_ENV
          echo "WINDOWS_SDK_VERSION=$selectedSdk" >> $env:GITHUB_ENV
          $sdkBinPath = "${env:ProgramFiles(x86)}\Windows Kits\10\bin\$selectedSdk\x64"
          if (Test-Path $sdkBinPath) { echo "$sdkBinPath" >> $env:GITHUB_PATH }
        }
        echo "=== Visual Studio 설치 확인 ==="
        Get-ChildItem "${env:ProgramFiles}\Microsoft Visual Studio\2022\" -ErrorAction SilentlyContinue
        Get-ChildItem "${env:ProgramFiles(x86)}\Microsoft Visual Studio\" -ErrorAction SilentlyContinue

    - name: Cache dependencies
      uses: actions/cache@v4
      with:
        path: |
          lib/
          deps/
        key: ${{ runner.os }}-deps-${{ hashFiles('**/HOWTOBUILD.md') }}
        restore-keys: |
          ${{ runner.os }}-deps-

    - name: Create directories
      run: |
        New-Item -ItemType Directory -Force -Path lib
        New-Item -ItemType Directory -Force -Path deps

    - name: Cache vcpkg
      uses: actions/cache@v4
      with:
        path: |
          vcpkg
          vcpkg/installed
        key: vcpkg-${{ matrix.platform }}-${{ hashFiles('vcpkg.json', 'vcpkg-configuration.json') }}
        restore-keys: |
          vcpkg-${{ matrix.platform }}-
          vcpkg-

    - name: Setup vcpkg
      run: |
        if (!(Test-Path "vcpkg")) { git clone https://github.com/Microsoft/vcpkg.git vcpkg }
        cd vcpkg
        if (!(Test-Path "vcpkg.exe")) { .\bootstrap-vcpkg.bat }
        echo "VCPKG_ROOT=$PWD" >> $env:GITHUB_ENV
        echo "$PWD" >> $env:GITHUB_PATH
        cd ..

    - name: Install detours and deps via vcpkg (manifest, x64)
      run: |
        .\vcpkg\vcpkg.exe install --triplet x64-windows
        echo "VCPKG_INSTALLED_DIR=$PWD\vcpkg\installed" >> $env:GITHUB_ENV
        echo "VCPKG_TARGET_TRIPLET=x64-windows" >> $env:GITHUB_ENV
        .\vcpkg\vcpkg.exe list
        if (Test-Path "$PWD\vcpkg\installed\x64-windows\lib\detours.lib") {
          Write-Host "detours.lib 확인됨: $PWD\\vcpkg\\installed\\x64-windows\\lib\\detours.lib" -ForegroundColor Green
        } else {
          Write-Host "경고: detours.lib 미발견 (vcpkg installed lib)" -ForegroundColor Yellow
        }

    - name: Build FreeType (custom fork for MacType) x64
      run: |
        Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser -Force
        $ftDir = Join-Path $PWD "deps/freetype"
        if (!(Test-Path $ftDir)) {
          git clone https://github.com/snowie2000/freetype.git $ftDir
        }
        # 선택: 필요한 경우 MacType 패치 적용 (존재 시에만)
        if (Test-Path (Join-Path $PWD "doc/glyph_to_bitmapex.diff")) {
          try { git -C $ftDir apply (Join-Path $PWD "doc/glyph_to_bitmapex.diff") } catch { Write-Host "패치 적용 건너뜀: $($_.Exception.Message)" -ForegroundColor Yellow }
        }
        $sdkVersion = $env:WINDOWS_SDK_VERSION; if (-not $sdkVersion) { $sdkVersion = "10.0.26100.0" }
        $ftSln = Join-Path $ftDir "builds/windows/vc2010/freetype.sln"
        if (Test-Path $ftSln) {
          msbuild $ftSln -p:Configuration=Release -p:Platform=x64 -p:WindowsTargetPlatformVersion=$sdkVersion -p:PlatformToolset=v143 -v:minimal
          $out1 = Join-Path $ftDir "objs/x64/Release/freetype.lib"
          $out2 = Join-Path $ftDir "objs/AMD64/Release/freetype.lib"
          $picked = $null
          if (Test-Path $out1) { $picked = $out1 } elseif (Test-Path $out2) { $picked = $out2 } else {
            $cand = Get-ChildItem -Path $ftDir -Recurse -Include freetype.lib -ErrorAction SilentlyContinue | Select-Object -First 1
            if ($cand) { $picked = $cand.FullName }
          }
          if ($picked) {
            Copy-Item $picked (Join-Path $PWD "lib/freetype.lib") -Force
            Write-Host "freetype.lib 생성: $picked -> lib/freetype.lib" -ForegroundColor Green
          } else {
            Write-Host "경고: freetype.lib 산출물 찾기 실패" -ForegroundColor Yellow
          }
        } else {
          Write-Host "경고: FreeType VS 솔루션 파일을 찾을 수 없습니다: $ftSln" -ForegroundColor Yellow
        }

    - name: Build modern dependencies (vcpkg-based)
      run: |
        Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser -Force
        .\scripts\build-modern-dependencies.ps1 -Platform ${{ matrix.platform }} -Configuration Release
        echo "CMAKE_TOOLCHAIN_FILE=$PWD/vcpkg/scripts/buildsystems/vcpkg.cmake" >> $env:GITHUB_ENV
        if (Test-Path "$PWD\lib\detours.lib") { Write-Host "lib\\detours.lib 확인됨" -ForegroundColor Green } else { Write-Host "경고: lib\\detours.lib 없음" -ForegroundColor Yellow }
        if (!(Test-Path "$PWD\lib\freetype.lib") -and (Test-Path "$PWD\vcpkg\installed\x64-windows\lib\freetype.lib")) { Copy-Item "$PWD\vcpkg\installed\x64-windows\lib\freetype.lib" "$PWD\lib\freetype.lib" -Force; Write-Host "freetype.lib 복사 완료(vcpkg)" -ForegroundColor Green } else { if (!(Test-Path "$PWD\lib\freetype.lib")) { Write-Host "경고: freetype.lib 미발견 (fork/vcpkg 모두)" -ForegroundColor Yellow } }
        dir lib | findstr detours || echo "lib 폴더 내용 나열 완료"

    - name: Build IniParser (x64) and copy lib
      run: |
        Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser -Force
        $inipDir = Join-Path $PWD "deps/iniparser"
        if (!(Test-Path $inipDir)) {
          git clone https://github.com/snowie2000/IniParser.git $inipDir
        }
        $sln = $null
        $cands = @("IniParser.sln","src/IniParser.sln","IniParser/IniParser.sln")
        foreach ($c in $cands) {
          $p = Join-Path $inipDir $c
          if (Test-Path $p) { $sln = $p; break }
        }
        if (-not $sln) {
          $proj = Get-ChildItem -Path $inipDir -Recurse -Filter *.vcxproj -ErrorAction SilentlyContinue | Select-Object -First 1
          if ($proj) { $sln = $proj.FullName } else { throw "IniParser 솔루션/프로젝트를 찾을 수 없습니다" }
        }
        $sdkVersion = $env:WINDOWS_SDK_VERSION; if (-not $sdkVersion) { $sdkVersion = "10.0.26100.0" }
        msbuild $sln -p:Configuration=Release -p:Platform=x64 -p:WindowsTargetPlatformVersion=$sdkVersion -p:PlatformToolset=v143 -v:minimal
        # 산출물 위치/이름 자동 탐색 (대소문자/경로 다양성 대응)
        $candidates = Get-ChildItem -Path $inipDir -Recurse -Include *.lib -ErrorAction SilentlyContinue
        $picked = $null
        foreach ($f in $candidates) {
          if ($f.FullName -match "IniParser" -and ($f.FullName -match "x64\\Release" -or $f.FullName -match "\\x64\\")) { $picked = $f.FullName; break }
        }
        if (-not $picked -and $candidates) { $picked = $candidates[0].FullName }
        if ($picked) {
          Copy-Item $picked (Join-Path $PWD "lib/iniparser64.lib") -Force
          Write-Host "iniparser64.lib 생성: $picked -> lib/iniparser64.lib" -ForegroundColor Green
        } else {
          Write-Host "경고: IniParser 산출물(.lib)을 찾지 못했습니다" -ForegroundColor Yellow
        }

    - name: Build MacType (Rel+Detours x64)
      run: |
        $msbuildPlatform = "x64"
        $sdkVersion = $env:WINDOWS_SDK_VERSION; if (-not $sdkVersion) { $sdkVersion = "10.0.26100.0" }
        echo "사용할 Windows SDK 버전: $sdkVersion"
        msbuild ${{ env.SOLUTION_FILE_PATH }} -p:Configuration="Rel+Detours" -p:Platform=$msbuildPlatform -p:WindowsTargetPlatformVersion=$sdkVersion -p:UseMultiToolTask=true -p:EnforceProcessCountAcrossBuilds=true /v:n /fl "/flp:logfile=msbuild-RelDetours.log;verbosity=normal"

    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: mactype-x64-builds
        path: |
          Release/**
          Rel+Detours/**
          **/*.dll
          **/*.exe
          msbuild-RelDetours.log
        retention-days: 30
